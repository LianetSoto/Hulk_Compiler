[package]
name = "hulk_compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "1.0"
inkwell = { version = "0.4", features = ["llvm17-0"] }
lalrpop-util = { version = "0.23.0", features = ["lexer"] }

[build-dependencies]
lalrpop = "0.23.0"

[features]
default = []
dev = []